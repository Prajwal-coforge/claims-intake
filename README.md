# Claims Intake Service

The service, its tests, and its docs live under `claims-intake/`, not at the
repository root.

```
cd claims-intake
```

Then follow `claims-intake/README.md` for running the service, running the
tests, and building the image.
